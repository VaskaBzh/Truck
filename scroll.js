/* const anchor = document.querySelector('.office_img') */

/*for (let anchor of anchors) {
    anchor.addEventListener("click", function(event) {
        event.preventDefault();
        const blockID = anchor.getAttribute('href')
        document.querySelector('' + blockID).scrollIntoView({
            behavior: "smooth",
            block: "start"
        })
    })
}   */

/* const top = document.querySelector('.section_europe')

function setScrollIntoViewOptions(top) {
    const scroll = document.querySelector('.office_img');
    office_img.scrollIntoView ({
        behavior: "smooth",
        block: "start"
    })
} */

/* Я очень старался, но не смог разобраться, никакой прокрутки :( */